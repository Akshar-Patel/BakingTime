<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="match_parent"
  android:layout_height="match_parent"
  android:background="@color/colorPrimary">

  <TextView
    android:id="@+id/textViewRecipeName"
    android:layout_width="match_parent"
    android:layout_height="50dp"
    android:layout_alignParentTop="true"
    android:layout_centerHorizontal="true"
    android:background="@color/colorAccent"
    android:gravity="center"
    android:textColor="@android:color/white"
    android:textSize="@dimen/text_size_xlarge_default"/>
  <TextView
    android:id="@+id/appwidget_text"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="@dimen/margin_single_default"
    android:layout_below="@+id/textViewRecipeName"
    android:layout_centerHorizontal="true"
    android:contentDescription="@string/app_widget_text"
    android:text="@string/app_widget_text"
    android:textColor="@android:color/white"
    android:textSize="@dimen/text_size_large_default"/>


</RelativeLayout>